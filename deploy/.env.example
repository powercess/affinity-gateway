# Use the existing new-api Compose network and service name.
NEW_API_NETWORK=new-api_default
NEW_API_UPSTREAM=new-api:3000
# Loopback by default: expose via your existing TLS reverse proxy.
INBOUND_BIND=127.0.0.1
INBOUND_PORT=8236
